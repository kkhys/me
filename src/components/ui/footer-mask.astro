<div class="footer-mask" />

<style>
    .footer-mask {
        position: fixed;
        left: 0;
        bottom: 0;
        width: 100%;
        height: clamp(80px, 10vh, 200px);
        mask-image: linear-gradient(to top, rgb(0, 0, 0) 25%, transparent);
        backdrop-filter: blur(5px);
        pointer-events: none;
    }

    .footer-mask::before {
        content: '';
        position: absolute;
        inset: 0;
        background: linear-gradient(to top, var(--footer-mask), transparent);
    }
</style>
