---
import type { HTMLAttributes } from "astro/types";
import Heading from "./heading.astro";

interface Props extends HTMLAttributes<"h3"> {
  id: string;
}

const { id, ...rest } = Astro.props;
---

<Heading as="h3" id={id} {...rest}><slot /></Heading>
