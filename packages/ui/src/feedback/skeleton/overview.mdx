import { Canvas, Meta, Subtitle, Title } from '@storybook/blocks';

import * as SkeletonStories from './index.stories';

<Meta of={SkeletonStories} name='Overview' />
<Title />
<Subtitle>Use to show a placeholder while content is loading.</Subtitle>
<Canvas of={SkeletonStories.Default} />

```tsx
import { Skeleton } from '#/feedback';

export default () => (
  <div className='flex items-center space-x-4'>
    <Skeleton className='size-12 rounded-full' />
    <div className='space-y-2'>
      <Skeleton className='h-4 w-[250px]' />
      <Skeleton className='h-4 w-[200px]' />
    </div>
  </div>
);
```

## Anatomy

```tsx
import { Skeleton } from '#/feedback';

export default () => <Skeleton />;
```

# Reference

[shadcn/ui](https://ui.shadcn.com/docs/components/skeleton)
