<header>
  <div class="site-inner">
    <span class="brand">
      <a href="/">Trend Digest</a>
    </span>
    <nav class="menu" aria-label="サイト内リンク">
      <a href="/archive">Archive</a>
      <a href="/about">About</a>
    </nav>
  </div>
</header>

<style>
  .site-inner {
    max-width: 42rem;
    margin: 0 auto;
    padding: 0.625rem 1rem;
    display: flex;
    align-items: center;
  }

  /* Keep the brand aligned with the content column edge (see digest-view). */
  @media (width >= 640px) {
    .site-inner {
      padding-inline: 0;
    }
  }

  .brand {
    font-size: var(--fs-xs);
    font-weight: var(--fw-medium);
  }

  .menu {
    margin-left: auto;
    display: flex;
    gap: 1rem;
    font-size: var(--fs-xs);
  }

  .menu a {
    color: var(--c-sub);
  }

  .menu a:hover {
    color: var(--c-text);
  }
</style>
