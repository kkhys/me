import { Canvas, Controls, Meta, Subtitle, Title } from '@storybook/blocks';

import * as SliderStories from './index.stories';

<Meta of={SliderStories} name='Overview' />
<Title />
<Subtitle>An input where the user selects a value from within a given range.</Subtitle>
<Canvas of={SliderStories.Default} />
<Controls of={SliderStories.Default} />

```jsx
import { Slider } from '#/ui/data-entry';

export default () => <Slider defaultValue={[50]} max={100} step={1} />;
```

## When To Use

To input a value in a range.

## Anatomy

Import the component.

```jsx
import { Slider } from '#/ui/data-entry';

export default () => <Slider />;
```

## Reference

- [shadcn/ui](https://ui.shadcn.com/docs/components/slider)
- [Radix Primitives](https://www.radix-ui.com/primitives/docs/components/slider)
