import { Canvas, Controls, Meta, Subtitle, Title } from '@storybook/blocks';

import * as ToastStories from './index.stories';

<Meta of={ToastStories} name='Overview' />
<Title />
<Subtitle>A succinct message that is displayed temporarily.</Subtitle>
<Canvas of={ToastStories.Default} layout='fullscreen' />
<Controls of={ToastStories.Default} />

```tsx
'use client';

import { ToastAction, useToast } from '#/ui/feedback';
import { Button } from '#/ui/general';

export default () => {
  const { toast } = useToast();

  return (
    <Button
      variant='outline'
      onClick={() => {
        toast({
          title: 'Scheduled: Catch up ',
          description: 'Friday, February 10, 2023 at 5:57 PM',
          action: <ToastAction altText='Goto schedule to undo'>Undo</ToastAction>,
        });
      }}
    >
      Add to calendar
    </Button>
  );
};
```

## Anatomy

```tsx
import {
  Toast,
  ToastAction,
  ToastClose,
  ToastDescription,
  ToastProvider,
  ToastTitle,
  ToastViewport,
} from '#/ui/feedback';

export default () => (
  <ToastProvider>
    <Toast>
      <ToastTitle />
      <ToastDescription />
      <ToastAction />
      <ToastClose />
    </Toast>
    <ToastViewport />
  </ToastProvider>
);
```

## Examples

### Simple

<Canvas of={ToastStories.Simple} layout='fullscreen' />

```tsx
'use client';

import { useToast } from '#/ui/feedback';
import { Button } from '#/ui/general';

export default () => {
  const { toast } = useToast();

  return (
    <Button
      variant='outline'
      onClick={() => {
        toast({
          description: 'Your message has been sent.',
        });
      }}
    >
      Show Toast
    </Button>
  );
};
```

### With Title

<Canvas of={ToastStories.WithTitle} layout='fullscreen' />

```tsx
'use client';

import { useToast } from '#/ui/feedback';
import { Button } from '#/ui/general';

export default () => {
  const { toast } = useToast();

  return (
    <Button
      variant='outline'
      onClick={() => {
        toast({
          title: 'Uh oh! Something went wrong.',
          description: 'There was a problem with your request.',
        });
      }}
    >
      Show Toast
    </Button>
  );
};
```

### With Action

<Canvas of={ToastStories.WithAction} layout='fullscreen' />

```tsx
'use client';

import { ToastAction, useToast } from '#/ui/feedback';
import { Button } from '#/ui/general';

export default () => {
  const { toast } = useToast();

  return (
    <Button
      variant='outline'
      onClick={() => {
        toast({
          title: 'Uh oh! Something went wrong.',
          description: 'There was a problem with your request.',
          action: <ToastAction altText='Try again'>Try again</ToastAction>,
        });
      }}
    >
      Show Toast
    </Button>
  );
};
```

### Destructive

<Canvas of={ToastStories.Destructive} layout='fullscreen' />

Use `toast({ variant: "destructive" })` to display a destructive toast.

```tsx
'use client';

import { ToastAction, useToast } from '#/ui/feedback';
import { Button } from '#/ui/general';

export default () => {
  const { toast } = useToast();

  return (
    <Button
      variant='outline'
      onClick={() => {
        toast({
          variant: 'destructive',
          title: 'Uh oh! Something went wrong.',
          description: 'There was a problem with your request.',
          action: <ToastAction altText='Try again'>Try again</ToastAction>,
        });
      }}
    >
      Show Toast
    </Button>
  );
};
```

## Reference

- [shadcn/ui](https://ui.shadcn.com/docs/components/toast)
- [Radix Primitives](https://www.radix-ui.com/primitives/docs/components/toast)
- [ARIA design pattern](https://www.w3.org/TR/wai-aria/#aria-live)
