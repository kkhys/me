import { Canvas, Controls, Meta, Subtitle, Title } from '@storybook/blocks';

import * as ProgressStories from './index.stories';

<Meta of={ProgressStories} name='Overview' />
<Title />
<Subtitle>
  Displays an indicator showing the completion progress of a task, typically displayed as a progress bar.
</Subtitle>
<Canvas of={ProgressStories.Default} />
<Controls of={ProgressStories.Default} />

```tsx
'use client';

import * as React from 'react';

import { Progress } from '#/ui/feedback';

export default () => {
  const [progress, setProgress] = React.useState(13);

  React.useEffect(() => {
    const timer = setTimeout(() => setProgress(66), 500);
    return () => clearTimeout(timer);
  }, []);

  return <Progress value={progress} className='w-[60%]' />;
};
```

## Anatomy

```tsx
import { Progress } from '#/ui/feedback';

export default () => <Progress />;
```

## Reference

- [shadcn/ui](https://ui.shadcn.com/docs/components/progress)
- [Radix Primitives](https://www.radix-ui.com/primitives/docs/components/progress)
- [ARIA design pattern](https://www.w3.org/WAI/ARIA/apg/patterns/meter)
