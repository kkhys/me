/** @typedef  { import('prettier').Config } PrettierConfig */

/** @type { PrettierConfig } */

const config = {
  jsxSingleQuote: true,
  singleQuote: true,
  trailingComma: 'all',
};

export default config;
